# 📋 Review Conference Report: Pipeline Explainer Validation Roadmap

**Subject**: Creating a core validation tasklist based on `pipeline-explainer.md`
**Panel**: 
1. `/06-audit` (Evidence & Reality)
2. `/17-lattice-review` (Architecture & Layering)
3. `/01-senior-reviewr` (Chair & Synthesizer)

---

### Panel Input Summary

#### 🕵️‍♂️ `/06-audit` (Evidence & Reality)
- **Key Insight 1**: The Explainer clearly states Step 1 (Background Habit Parsing) and Step 5 (RL Engine Write-Backs) are `[WIP]/Deferred`. You cannot definitively "validate" the pipeline until these missing engines are physically built and wired. 
- **Key Insight 2**: Step 2 (The Gateway) claims it halts the pipeline on ambiguity. We proved this in L3 *accidentally* (the duplicate bubble bug). We need an intentional, scriptable L2/L3 barrage dataset to prove the boundaries of this gateway dynamically.

#### 🏗️ `/17-lattice-review` (Architecture)
- **Key Insight 1**: The 5 steps cross exactly from OS:App (Step 1) → OS:RAM (Step 3) → OS:Kernel (Step 4) → OS:SSD (Step 5). Validation must not be monolithic. It must test the *handoff* interfaces.
- **Key Insight 2**: To validate Step 3 (RAM Assembly), we need to prove that the `PipelineContext` package is strictly pure JSON and doesn't accidentally pull in Android/Platform dependencies or leak context between concurrent turns.

---

### 💡 `/01-senior-reviewr` Synthesis

**The Verdict**: "Validating the pipeline" isn't about writing more assertions; it's about pushing the engine to its limits and finishing the delayed components so the OS Model is whole. 

Right now, the Explainer is a map of a 5-cylinder engine, but cylinder 5 (the RL Engine) is hollow. Our next Epic must be split into two halves: **Completing the Missing Engines** and then **Stress-Testing the Whole**.

Here is the proposed, pragmatic Tasklist (Epic Roadmap) for validating `pipeline-explainer.md`.

---

## 🎯 Proposed Active Epic: "The Crucible" (Pipeline Validation)

> **Objective**: Systematically build out the `[WIP]` components of the `pipeline-explainer.md` and subject the 5-step engine to extreme chaos to prove it never hallucinates.

### 🌊 Wave 1: Completing the Missing Twin (Step 5)
*Target: Replace the Fake RL Engine with the Real RL Subsystem.*
- [ ] Build `RealHabitListener` (The Background Path in Step 1).
- [ ] Wire the RL Engine write-back loop (Step 5).
- [ ] Verify `loadUserHabits()` organically feeds back into Step 3 (RAM Assembly) on the *next* turn.

### 🌊 Wave 2: The Gateway Gauntlet (Step 2)
*Target: Prove the Disambiguator cannot be tricked.*
- [ ] Use `WorldStateSeeder` to inject 10 identically named companies, but with different geographies.
- [ ] Run L2 Simulation queries forcing the LLM to disambiguate based on location/history instead of raw name.
- [ ] Explicitly verify the `Intercept & Resume` loop handles deep contextual ambiguity.

### 🌊 Wave 3: Context Poisoning Defense (Step 3)
*Target: Prove the RAM Assembly only gives the LLM exactly what it needs, and nothing more.*
- [ ] Inject massive, irrelevant `noise` into a client's SSD history.
- [ ] Audit the `PipelineContext` JSON payload generated by `RealContextBuilder`. Prove it compresses/lazy-loads correctly without blowing up the token window or distracting the LLM.

### 🌊 Wave 4: Write-Back Concurrency (Step 5)
*Target: Prove the Twin Engines (CRM + RL) don't overwrite each other in RAM or SSD.*
- [ ] Send back-to-back rapid-fire intents ("Change their address" immediately followed by "Remind me to call them").
- [ ] Verify the pipeline handles queued writes and the `SessionWorkingSet` stays perfectly synchronized with the SSD.

---

### 🔴 Hard No (What NOT to do)
- Do not build a massive E2E UI testing suite. Rely on L2 Mock-Evicted Simulation for the heavy lifting.
- Do not "fake" the RL Engine for the context poisoning test. We need to see the real data flow.

### 🟢 Good Calls
- Basing the next Epic *directly* on the Explainer Artifact is exactly how we prevent drift. The Explainer is the contract; this Epic proves the contract.
