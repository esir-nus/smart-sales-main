# Prism 架构规范 (V1) - 总览

> **原文链接**: [Architecture.md](../../specs/Architecture.md)
> **状态**: Draft / Active
> **日期**: 2026-01-21
> **继承自**: Lattice 架构

---

## 1. 执行摘要 (Executive Summary)

**Prism** 是智能个人助理系统的架构标准。它用一个 **统一管道 (Unified Pipeline)** 取代了复杂的状态协调，并将具体行为委托给 **模式策略 (Mode Strategies)**。

"Prism" (三棱镜) 这个名字反映了核心模式：单一的用户意图流进入系统，被 Orchestrator (编排器) "折射" 到特定的执行路径 (Chat, Analyze, Schedule) 中，同时共享 Context (上下文)、Memory (记忆) 和 Intelligence (智能) 的通用基础。

### 核心原则
1.  **统一管道**: 所有模式共享相同的高级流程 (Context → Execute → Publish → Persist)。
2.  **策略模式**: 模式特定的逻辑 (LLM 选择, 渲染, 工具) 封装在可切换的策略中。
3.  **即发即弃 (Fire-and-Forget) 持久化**: 记忆写入在后台发生。UI **从不** 阻塞等待数据库操作。
4.  **流式优先**: 架构围绕原生流式传输 (DashScope Flow<Token>) 构建，以实现最小延迟。

---

## 2. 架构蓝图 (Architecture Blueprint)

### 2.1 统一编排管道

```
┌─────────────────────────────────────────────────────────────────────┐
│                    UNIFIED ORCHESTRATION PIPELINE                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────┐   ┌──────────────┐   ┌───────────────┐   ┌──────────┐ │
│  │ Context │──▶│   Executor   │──▶│   Publisher   │──▶│   UI     │ │
│  │ Builder │   │    (LLM)     │   │   (Per Mode)  │   │          │ │
│  └─────────┘   └──────────────┘   └───────────────┘   └──────────┘ │
│       │               │                   │                         │
│       │               │                   │      ┌──────────────┐  │
│       │               │                   └─────▶│ Tool Agents  │  │
│       │               │                          │ (If Needed)  │  │
│       │               │                          └──────────────┘  │
│       │               │                                             │
│       │               ▼                                             │
│       │        [Memory Writer] ──fire & forget──▶ [Hot Zone]       │
│       │                                                             │
│       ▼                                                             │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    MODE STRATEGY MATRIX                      │   │
│  ├────────────┬───────────────┬──────────────────┬─────────────┤   │
│  │ Component  │    CHAT       │     ANALYZE      │  SCHEDULE   │   │
│  ├────────────┼───────────────┼──────────────────┼─────────────┤   │
│  │ Executor   │ Lightweight   │ Reasoning Model  │ Structured  │   │
│  │ (LLM)      │ (qwen-turbo)  │ (qwen-max3)      │ Output LLM  │   │
│  ├────────────┼───────────────┼──────────────────┼─────────────┤   │
│  │ Publisher  │ ChatPublisher │ AnalystPublisher │ SchedPub    │   │
│  │            │ (stream +     │ (format chapters │ (calendar   │   │
│  │            │  context      │  insights, call  │  entry +    │   │
│  │            │  aware)       │  chart tools)    │  smart tips)│   │
│  ├────────────┼───────────────┼──────────────────┼─────────────┤   │
│  │ Tools      │ None          │ Charts, Reports  │ Calendar,   │   │
│  │            │               │ Recommendations  │ Tip Writer  │   │
│  └────────────┴───────────────┴──────────────────┴─────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```
