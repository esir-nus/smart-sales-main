You are acting as the editor of docs/ui-ux/assistant-ux-contract.md.

Task:
1. Read the current content of assistant-ux-contract.md.
2. Read the following new UX changes and decisions (I will paste them after this prompt).
3. Update the doc to reflect these changes at a **high-level only**:
   - Adjust principles if they are outdated.
   - Add or update entries in the Decision Log.
   - Do NOT add pixel-level specs, component names, or test tags.
   - Do NOT re-introduce React / legacy UI as source of truth.
   - Keep the priority order: this doc > live Compose UX > style-guide (visual only) > others.

Rules:
- Preserve the structure and tone of the existing doc.
- Keep changes minimal but precise: update only the sections that are truly affected.
- Never remove the “Priority & Relationships” section.
- Never weaken the “one-time auto rename, then user owns the title” rule.

Now I’ll paste the new UX changes. After reading them, output the **updated full text** of assistant-ux-contract.md.
