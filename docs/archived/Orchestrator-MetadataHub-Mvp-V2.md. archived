# Orchestrator + MetaHub MVP (v2)
此文档已被 Orchestrator-MetadataHub-Mvp-V3 替代，仅供历史参考
*Chat · Tingwu · Export (PDF/CSV) · Analysis-first pipeline*

本文包含两层：
1) **规范性合约**：描述 Orchestrator + MetaHub 的目标行为（必须/应该）。
2) **当前实现 & Audit 记录**：描述当前代码与合约的对齐情况，便于回归检查。

---

## 1. Purpose & Core Principles

### 1.1 Purpose & Scope

本规范覆盖 Orchestrator + MetaHub 栈，支撑：

* Home chat（GENERAL_CHAT, SMART_ANALYSIS）
* Tingwu diarized transcripts（speaker relabeling）
* Export（PDF/CSV），遵循 **analysis-first** 逻辑

### 1.2 Core Principles

1. **LLM front door = Orchestrator(s)**
   * 任何 LLM 调用必须经过 Orchestrator（HomeOrchestrator / TranscriptOrchestrator）。
   * Export 全程 LLM-free。
2. **MetaHub 作为 metadata blackboard**
   * Session/Transcript/Export/TokenUsage/CRM 数据集中存储，UI 通过 helper 读取。
3. **Adapter first, brains later**
   * 先封装现有流程，再逐步收拢 prompt/解析/metadata 写入。
4. **Safe merge & safe fallback**
   * 只用非空新值覆盖旧值；缺失/解析失败时保持原有 UI 行为。
5. **Export is metadata-driven & LLM-free**
   * 分析只做一次（SMART_ANALYSIS）；导出仅消费 metadata 与历史内容。

---

## 2. Metadata Schema (MetaHub)

### 2.1 SessionMetadata（会话级）

* `sessionId: String`
* `mainPerson: String?`（例：“罗总”、“未知客户”）
* `shortSummary: String?`（1–2 句）
* `summaryTitle6Chars: String?`（4–8 字标题）
* `location: String?`（例：“上海”、“广州门店”）
* `stage: SessionStage?`（`DISCOVERY/NEGOTIATION/PROPOSAL/CLOSING/POST_SALE/UNKNOWN`）
* `riskLevel: RiskLevel?`（`LOW/MEDIUM/HIGH/UNKNOWN`）
* `tags: Set<String>`
* `lastUpdatedAt: Long`（ms）
* `latestMajorAnalysisMessageId: String?`
* `latestMajorAnalysisAt: Long?`（ms）
* `latestMajorAnalysisSource: AnalysisSource?`（如 `GENERAL_FIRST_REPLY/SMART_ANALYSIS_USER/SMART_ANALYSIS_AUTO`）
* `crmRows: List<CrmRow>`（可为空列表）

合并规则：仅当新值非空/非默认时覆盖旧值；缺失字段不得抹掉已有值。

### 2.2 TranscriptMetadata & SpeakerMeta（Tingwu）

* `transcriptId: String`
* `sessionId: String?`
* `speakerMap: Map<String, SpeakerMeta>`
* `source: TranscriptSource`（如 `TINGWU/TINGWU_LLM/UNKNOWN`）
* `createdAt: Long`（ms）
* `diarizedSegmentsCount: Int`
* `extra: Map<String, Any?>`

`SpeakerMeta`：

* `displayName: String?`（例：“罗总”、“销售顾问”）
* `role: SpeakerRole?`（`CUSTOMER/SALES/OTHER/UNKNOWN`）
* `confidence: Float?`（0–1）

### 2.3 ExportMetadata

* `sessionId: String`
* `lastPdfFileName: String?`
* `lastPdfGeneratedAt: Long?`（ms）
* `lastCsvFileName: String?`
* `lastCsvGeneratedAt: Long?`（ms）

### 2.4 TokenUsage（可选，仅日志）

* `sessionId: String?`
* `model: String?`
* `tokensIn: Int?`
* `tokensOut: Int?`
* `latencyMs: Long?`
* `timestamp: Long`（ms）

### 2.5 Schema 演进规则

* 新增字段必须可空/optional，调用方在 null 时仍要正常工作。
* 合并采用「非空覆盖」策略，不得用空值覆盖已有值。
* 废弃字段先标记 deprecated（代码与文档共存 1–2 个版本），确认无依赖后再移除。
* 导出/解析层必须对未知字段前向兼容，忽略未识别键。

---

## 3. Orchestrators & Modes（规范层）

### 3.1 HomeOrchestrator（GENERAL_CHAT / SMART_ANALYSIS）

Modes：`GENERAL_CHAT`、`SMART_ANALYSIS`、`TRANSCRIPT_METADATA`（仅透传）、`EXPORT_PDF`、`EXPORT_CSV`。

GENERAL_CHAT / SMART_ANALYSIS 规范：

* 首次回复必须包含四个 markdown 段落：
  1. 客户画像
  2. 客户需求
  3. 销售建议
  4. 总结（含核心洞察 + 精准一句话）
* 必须附带 JSON（见下方 schema），缺失时系统仍需安全降级。
* 输入分桶：`NOISE`（本地处理）、`SHORT_RELEVANT`（澄清导向 prompt）、`RICH`（直接分析）。
* Orchestrator 解析 JSON，合并写入 SessionMetadata（含 crmRows/最新分析标记），并保持流式事件向 UI 透传。
* 可以存在额外的“首次回复本地解析”安全兜底，但核心写入职责在 Orchestrator。

```jsonc
// Chat metadata schema (GENERAL_CHAT / SMART_ANALYSIS)
{
  "main_person": "string, optional",
  "short_summary": "string, optional",           // 建议 ≤20 字
  "summary_title_6chars": "string, optional",    // 建议 ≤6 汉字
  "location": "string, optional",                // 例: "上海线下会议" / "线上会议（城市未知）"
  "highlights": ["string"],
  "actionable_tips": ["string"],
  "summary": {                                   // 可选，便于 UI 展示核心洞察
    "core_insight": "string",
    "sharp_line": "string"
  },
  "crm_rows": [
    {
      "client": "string",
      "region": "string",
      "stage": "string",
      "progress": "string",
      "next_step": "string",
      "owner": "string"
    }
  ]
}
```

字段要求：

* `main_person/short_summary/summary_title_6chars/location` 强烈建议输出，但缺失时系统必须回退。
* `crm_rows` 可为空列表；不得因缺失而崩溃。

### 3.2 TranscriptOrchestrator（TRANSCRIPT_METADATA / Tingwu）

职责：基于 Tingwu diarized segments 推理说话人、主讲人、摘要等，支持缓存命中与强制刷新。

规范要点：

* 先查 MetaHub，若已有非默认 speakerMap 且未强制刷新，可直接返回缓存。
* 未命中或强制刷新时调用 LLM，对抽样后的段落（覆盖全部说话人，每段 ≤200 字，最多 30 段）推理。
* 输出 JSON 解析后写入 TranscriptMetadata，并按非空合并补充 SessionMetadata（如 main_person/summary）。
* 显示层采用置信度阈值（如 0.6）决定是否覆盖原始标签；低置信度可选择保留 Tingwu 标签。

```jsonc
// Transcript metadata schema (TRANSCRIPT_METADATA)
{
  "speaker_map": {
    "<speakerId>": {
      "display_name": "string, optional",       // 简称，如 "罗总" / "销售顾问"
      "role": "string, optional",               // 自由文本，例: "客户决策人"
      "confidence": 0.0                         // 0~1，小数，可缺失
    }
  },
  "main_person": "string, optional",
  "short_summary": "string, optional",
  "summary_title_6chars": "string, optional",
  "location": "string, optional",
  "stage": "string, optional",                  // 可映射到 SessionStage
  "risk_level": "string, optional"              // 可映射到 RiskLevel
}
```

字段约定：

* `speaker_map` 缺失/为空时必须回退 Tingwu 原始 label。
* `confidence` 视为 [0,1]；解析层可 clamp；阈值配置化，低置信度可不覆盖。

### 3.3 ExportOrchestrator（EXPORT_PDF / EXPORT_CSV）

API：`exportMarkdown(sessionId, markdown, format)`，`format` 为 `PDF` 或 `CSV`。

规范要点：

* 文件名：`yyyyMMdd_HHmm_<mainPerson>_<summaryTitle>.<ext>`，非法字符需清理，过长需截断；从 SessionMetadata 获取 `mainPerson/summaryTitle6Chars/lastUpdatedAt`，缺失则回退旧逻辑。
* PDF：输入为分析 markdown；成功后写 ExportMetadata。
* CSV：来源仅限 SessionMetadata.crmRows；空列表时至少输出 header 或一行占位，不得抛异常；成功后写 ExportMetadata。
* Export 不调用 LLM。

---

## 4. UI Behavior & Export Macro

### 4.1 UI 行为摘要

* GENERAL_CHAT：四段结构 + JSON；NOISE 输入本地应答，其他走 Orchestrator；session 标题格式 `MM/dd_<mainPerson>_<summaryTitle>`，缺失时可用 heuristics。
* SMART_ANALYSIS：同样四段结构 + JSON，可由用户或导出按钮触发；更新 SessionMetadata（含 crmRows/最新分析标记）。
* Tingwu：高置信度 speakerMap 覆盖显示，低置信度保留原标签。
* Export：统一走 ExportOrchestrator；文件名由 metadata 驱动，fallback 保证可用。

### 4.2 Analysis-First Export Macro（PDF/CSV）

* 若已有 `latestAnalysisMarkdown`：直接调用 `exportMarkdown(sessionId, markdown, format)`。
* 若缺失：自动触发 SMART_ANALYSIS（以“导出前自动分析”为目标，优先长内容 ≥240 字）。分析完成后立刻执行导出。
* 分析成功时写入 SessionMetadata 的最新分析字段，并缓存 markdown 供导出使用。

### 4.3 失败场景预期行为

| 场景 | 预期行为 |
| --- | --- |
| GENERAL_CHAT/SMART_ANALYSIS JSON 解析失败 | UI 仍显示完整 markdown；不更新 SessionMetadata；记录调试日志 |
| TRANSCRIPT_METADATA JSON 解析失败 | 使用 Tingwu 原始 speaker label；不更新 TranscriptMetadata；记录调试日志 |
| MetaHub 写入失败（任意模式） | 不影响当前 UI 渲染；记录日志/Debug note；不崩溃 |
| 导出时缺 latestMajorAnalysis 或 crmRows | 触发自动 SMART_ANALYSIS 或走旧导出命名逻辑；导出不崩溃 |
| 缺失 speaker_map 或低置信度 | 回退原始标签，仍可展示/导出 |

---

## 5. Development Rules & Checkpoints

1. **No direct LLM calls**：仅 Orchestrator 触达 LLM。
2. **MetaHub writes**：仅 Orchestrator/特定 helper 负责；写入遵循非空合并。
3. **Tests**：断言结构/contract，不做脆弱字符串比对；使用 fakes（MetaHub/Orchestrator/Export）。
4. **模块边界**：低层不依赖 `feature/chat`；导出保持 LLM-free。
5. **Debug**：结构化日志 `event=...`；Debug HUD 仅在 Debug 构建可见；Release 不露调试字符串。

---

## 6. Implementation Status

| Component | Status | Notes |
|-----------|--------|-------|
| GENERAL_CHAT | ✅ Implemented | Prompt 强制四段 + JSON；输入分桶；metadata 解析与合并写入 |
| SMART_ANALYSIS | ✅ Implemented | 四段 + JSON；支持 CRM 行；标记最新分析来源 |
| SessionMetadata | ✅ Implemented | 含 `latestMajorAnalysis*`、`crmRows`，`lastUpdatedAt` 为 Long(ms)，`tags` 为 Set |
| TranscriptMetadata | ✅ Implemented | 字段名 `speakerMap`，含 `diarizedSegmentsCount/extra` |
| TranscriptOrchestrator | ✅ Implemented | `TranscriptMetadataRequest` 包装；缓存命中优先；LLM 推理 + 写回 |
| Tingwu → MetaHub | ✅ Implemented | 任务完成后写 TranscriptMetadata，`transcriptId` = Tingwu `taskId` |
| ExportOrchestrator | ⚠️ Partial | CSV 基于 crmRows；分析优先使用本地 `latestAnalysisMarkdown` 而非 messageId 拉取 |
| Debug Logging & HUD | ✅ Implemented | 结构化日志 + Debug HUD（Debug 构建 gated） |
| Session Title Generation | ✅ Implemented | 标题格式 `MM/dd_<mainPerson>_<summaryTitle>` |

---

## 7. Audit & Sync

**审计记录（已对齐代码）**

* 核对文件：`feature/chat/home/HomeScreenViewModel.kt`、`feature/chat/home/orchestrator/HomeOrchestratorImpl.kt`、`feature/chat/home/orchestrator/HomeOrchestrator.kt`、`core/metahub/*`、`data/ai-core/RealTingwuCoordinator.kt`、`data/ai-core/RealTranscriptOrchestrator.kt`、`data/ai-core/RealExportOrchestrator.kt`
* 关键校验：
  * Export 文件名 `yyyyMMdd_HHmm_<mainPerson>_<summaryTitle>`，CSV 读取 `SessionMetadata.crmRows`，空列表输出 header-only。
  * Tingwu `taskId` 用作 `transcriptId`；`speakerMap` 字段名已确认；缓存命中策略存在。
  * 分析优先导出：本地 `latestAnalysisMarkdown` 缓存路径已实现。
* Known divergences（文档已注明）：导出分析来源使用本地缓存而非 messageId 拉取；GENERAL_CHAT 解析存在双路径兜底。
* Future consolidation：可合并 GENERAL_CHAT 解析路径；导出可切换到 `latestMajorAnalysisMessageId` 溯源。

**Audit Summary（2025-01-28）**

* 最近审计确认上述实现与合约一致；关键类型与字段存在并匹配。
* 生产建议：在调整导出或 metadata 写入前，先复核合约章节是否仍适用；新增字段遵循 2.5 演进规则。
